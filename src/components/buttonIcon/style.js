import styled from "styled-components";
import tw from "twin.macro";

export const ButtonIconContainer = styled.button`
  ${tw`rounded-[10px] bg-primaryDefault p-[10px] cursor-pointer text-white`}
`;
