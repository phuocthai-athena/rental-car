import styled from "styled-components";
import tw from "twin.macro";

export const DropdownListContainer = styled.div`
  ${tw`absolute left-0 w-full bg-white border border-gray-200 rounded-lg  top-full`}
`;
