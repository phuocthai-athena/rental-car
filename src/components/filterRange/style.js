import styled from "styled-components";
import tw from "twin.macro";

export const InputRange = styled.input`
  ${tw`mb-2`}
`;

export const LabelRange = styled.label`
  ${tw`text-secondaryDefault text-sm tracking-[-0.02em] font-medium`}
`;
